import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  title: {
    width: '100%',
    fontSize: 30,
    fontWeight: 'bold',
    color: '#01AA4F'
  }
})

export default styles
