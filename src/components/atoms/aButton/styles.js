import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  buttonEnabled: {
    width: '100%',
    color: '#3fbdf1',
    fontSize: 26,
    textAlign: 'center',
    fontWeight: 'bold',
  },
  buttonDisabled: {
    width: '100%',
    color: 'gray',
    fontSize: 26,
    textAlign: 'center',
    fontWeight: 'bold',
    opacity: 0.6
  }
})

export default styles
